# 발표 스크립트 (4분 / 240초)

> 슬라이드와 같은 순서. 괄호 안은 권장 발표 시간.
> 발표자가 직접 외울 필요는 없고, 핵심 흐름과 페이스 가이드용.

---

## 발음 / 용어 가이드 (먼저 한 번 훑어보기)

| 용어 | 읽는 법 | 보충 설명 |
| -- | -- | -- |
| **He** 초기화 | **"헤"** 초기화 | Kaiming He(중국계 ML 연구자)의 이름. ReLU 표준 초기화 |
| **Xavier** 초기화 | **"자비에"** 초기화 | Xavier Glorot(프랑스 ML 연구자)의 이름. Sigmoid/tanh 표준 |
| Affine | "어파인" | 완전연결층 (`y = xW + b`) |
| ReLU | "렐루" | Rectified Linear Unit, 음수→0인 활성화 함수 |
| Softmax | "소프트맥스" | 확률 분포로 변환하는 출력 함수 |
| Cross Entropy | "크로스 엔트로피" | 분류 손실 함수 |
| BatchNorm | "배치놈" (= 배치 정규화) | 배치 단위 정규화 layer |
| Dropout | "드롭아웃" | 학습 중 일부 뉴런 무작위 차단 |
| Adam | "아담" | 모멘텀 + RMSProp 결합 옵티마이저 |
| epoch | "에폭" | 전체 학습 데이터를 한 번 다 본 단위 |
| batch | "배치" | 한 번에 학습하는 샘플 묶음 (여기선 128개) |
| gradient | "그래디언트" / "기울기" | 손실의 파라미터별 미분 |
| step decay | "스텝 디케이" | 학습률을 단계적으로 줄이는 방식 |
| in-place | "인플레이스" / "제자리" | 새 배열을 만들지 않고 같은 배열의 값을 직접 바꾸는 연산 |
| saturate | "새추레이트" / "포화" | 손실이 더 이상 줄지 않고 평탄해지는 상태 |

---

## 슬라이드 1 · 표지 (15초)

> 안녕하세요. **SW-AI 1조** 박지용, 김석제, 서원규, 김진호입니다.
> 저희는 MNIST 손글씨 인식 과제를 PyTorch나 TensorFlow 같은 프레임워크 없이, **NumPy만으로 신경망을 직접 구현**했고, 최종적으로 **Test 정확도 98.62%**를 달성했습니다.

---

## 슬라이드 2 · 과제 & 목표 (20초)

> MNIST는 손글씨 숫자 0부터 9까지 10개 클래스로 분류하는 문제입니다.
> 저희는 **Forward, Loss, Backward, Update 전 사이클을 직접 짜는 것**이 목표였고, Test 정확도 95% 이상, 권장 97% 이상을 목표치로 잡았습니다.
> 검증은 학습 정확도뿐 아니라 각 layer와 optimizer의 동작을 확인하는 **단위 테스트 21개**까지 모두 통과시키는 것을 기준으로 했습니다.

---

## 슬라이드 3 · 모델 구조 (30초)

> 모델은 2개의 은닉층을 가진 다층 퍼셉트론입니다.
> 입력 784 픽셀에서 시작해, **폭 1024의 어파인(Affine) layer → 배치놈(BatchNorm) → 렐루(ReLU) → 드롭아웃(Dropout) 0.4** 블록을 통과합니다.
> 같은 블록을 한 번 더, 이번엔 폭 512로 통과시킨 뒤, 마지막에 10개 클래스로 출력하는 어파인과 **소프트맥스(Softmax)**로 마무리합니다.
> 가중치 초기화는 **헤(He) 초기화** — Kaiming He의 이름을 딴, ReLU에 맞춘 표준 초기화 — 를 썼습니다. 총 파라미터는 약 **134만 개**입니다.

---

## 슬라이드 4 · 학습 설정 (30초)

> 학습 설정의 핵심은 **학습률 스케줄링**입니다.
> 옵티마이저는 아담(Adam), 배치 크기 128, 총 15 epoch입니다.
> epoch 1~10에서는 학습률 1e-3, **11~15에서는 1e-4로 한 번 줄이는 step decay 방식**으로 학습했습니다.
> 미니배치마다 Forward, Loss, Backward, Update 네 단계를 반복하는데, **소프트맥스와 크로스 엔트로피를 결합한 gradient를 직접 만들어** backward에 넘기는 패턴으로 구현했습니다.

---

## 슬라이드 5 · 학습 손실 — Baseline vs Best (35초)

> 학습 손실 그래프입니다. **회색이 baseline, 파란색이 best 구성**입니다.
> 두 곡선 모두 첫 epoch에서 가장 큰 감소를 보이고 단조 감소합니다.
> 주목할 부분은 **epoch 10과 11 사이**입니다. 이 시점에서 학습률을 1e-3에서 1e-4로 줄였는데, loss가 **0.0375에서 0.0257로, -31% 추가 감소**했습니다.
> 평소 같으면 후반에 새추레이트(saturate), 즉 포화되는 구간인데, 스케줄링 덕분에 추가 수렴 여력을 끌어낼 수 있었습니다. 마지막 epoch loss는 **0.0154**입니다.

---

## 슬라이드 6 · 최종 정확도 (25초)

> 최종 결과입니다.
> **Test 정확도 98.62%, Train 99.95%**로, 권장 목표 97%를 1.62%p 초과 달성했습니다.
> Train과 Test 격차는 1.33%p로 일반적인 정상 범위 안에 있고, 단위 테스트 21개도 모두 통과한 상태입니다.
> 학습 시간은 약 **23분**, 환경은 Python 3.11과 Windows 11 로컬 CPU입니다.

---

## 슬라이드 7 · Baseline vs Best 비교 (35초)

> baseline과 best를 비교해 봅니다.
> 저희는 이전 회고에서 향후 과제로 적었던 네 가지 — **드롭아웃 강화, 학습률 스케줄링, 은닉층 폭 변경, 헤(He)와 자비에(Xavier) 초기화 비교** — 중 효과가 클 것 같은 세 가지를 한 번에 적용했습니다.
> **은닉층 폭을 약 두 배로, 드롭아웃을 0.3에서 0.4로, 학습률은 step decay로** 바꿨고요.
> 자비에(Xavier) 초기화는 ReLU 신경망에서 헤(He)보다 이론적으로 불리해 단독 비교는 제외했습니다.
> 결과는 Test 정확도가 **98.41%에서 98.62%로 +0.21%p** 올랐습니다.

---

## 슬라이드 8 · 어떤 요소가 가장 기여했나 (20초)

> 세 가지 변경 중 어느 것이 가장 컸을까요.
> **첫째, 학습률 step decay가 가장 또렷한 효과**였습니다. 후반의 -31% 추가 감소가 이 덕분이고요.
> **둘째, 은닉층 폭 증가**는 capacity를 키웠지만 단독으로는 큰 차이가 없었고 다른 변수와 시너지를 냈습니다.
> **셋째, 드롭아웃 0.3에서 0.4**는 train/test 격차를 좁히는 데 기여했습니다.

---

## 슬라이드 9 · 구현하며 배운 핵심 (20초)

> 구현하며 배운 핵심은 네 가지입니다.
> 파라미터는 **제자리(in-place)로 갱신**해야 layer가 따라오고, **배치놈과 드롭아웃은 학습과 추론 동작이 다르며**, **소프트맥스와 크로스 엔트로피 결합 미분**은 `(y_pred − y_true)/N`으로 깔끔하게 정리되고, **shape 매칭이 backward 디버깅의 첫 관문**이라는 점입니다.

---

## 슬라이드 10 · 한계 & 향후 과제 (10초)

> 한계는 정확도 0.21%p를 위해 학습 시간이 약 6배 늘었다는 점, 그리고 CNN 없이 MLP로는 98.6%대가 사실상 상한이라는 점입니다.
> 다음에는 더 깊은 네트워크와 데이터 증강을 시도해 보겠습니다. **이상으로 발표 마치겠습니다. 감사합니다.**

---

## 시간 배분 합계

| 슬라이드   | 시간 (초) |
| ------ | ------ |
| 1 표지   | 15     |
| 2 과제·목표 | 20     |
| 3 모델 구조 | 30     |
| 4 학습 설정 | 30     |
| 5 손실 커브 | 35     |
| 6 정확도   | 25     |
| 7 비교    | 35     |
| 8 기여 요소 | 20     |
| 9 배운 점 | 20     |
| 10 마무리  | 10     |
| **합계**  | **240초 (4분)** |

---

## 발표 시 체크 포인트

- **속도**: 표지·마무리는 천천히, 모델 구조·학습 설정은 약간 빠르게 (정보량 多)
- **포인트 강조**: 그래프 보여줄 때 손가락/레이저로 **epoch 10→11 전환점**을 짚어주면 임팩트 ↑
- **숫자 외울 필요 없음**: 98.62%, 0.21%p, -31% 세 숫자만 자연스럽게
- **고유명사 읽기 통일**: 처음 등장할 때 "헤(He)", "자비에(Xavier)"처럼 한 번만 풀어 읽고, 이후엔 짧게
