# 발표 스크립트 (본 발표 1–8번 / 약 3분 25초, 9–11번 백업)

> 슬라이드와 같은 순서. 괄호 안은 권장 발표 시간.
> 핵심 서사: **Baseline → 회고에서 발견한 2가지 문제 → 1:1 해결 방법 2개 + 해결 방법 ②의 부작용을 보완하는 설계 페어링 1개 → v2 → 변경별 효과 회고**

---

## 발음 / 용어 가이드 (먼저 한 번 훑어보기)

| 용어 | 읽는 법 | 보충 설명 |
| -- | -- | -- |
| **He** 초기화 | **"헤"** 초기화 | Kaiming He(중국계 ML 연구자)의 이름. ReLU 표준 초기화 |
| **Xavier** 초기화 | **"자비에"** 초기화 | Xavier Glorot(프랑스 ML 연구자)의 이름. Sigmoid/tanh 표준 |
| **lr** / learning rate | "학습률" / "엘 알" | 한 번 업데이트 시 gradient에 곱해주는 값. 본 발표에선 lr ≈ "학습률" |
| **step decay** | "스텝 디케이" / "단계적 감소" | 학습률을 일정 epoch마다 한 번씩 줄이는 방식 |
| Affine | "어파인" | 완전연결층 (`y = xW + b`) |
| ReLU | "렐루" | Rectified Linear Unit, 음수→0인 활성화 함수 |
| Softmax | "소프트맥스" | 확률 분포로 변환하는 출력 함수 |
| Cross Entropy | "크로스 엔트로피" | 분류 손실 함수 |
| BatchNorm | "배치놈" (= 배치 정규화) | 배치 단위 정규화 layer |
| Dropout | "드롭아웃" | 학습 중 일부 뉴런 무작위 차단 |
| Adam | "아담" | 모멘텀 + RMSProp 결합 옵티마이저 |
| epoch | "에폭" | 전체 학습 데이터를 한 번 다 본 단위 |
| batch | "배치" | 한 번에 학습하는 샘플 묶음 (여기선 128개) |
| gradient | "그래디언트" / "기울기" | 손실의 파라미터별 미분 |
| in-place | "인플레이스" / "제자리" | 새 배열을 만들지 않고 같은 배열의 값을 직접 바꾸는 연산 |
| saturate | "새추레이트" / "포화" | 손실이 더 이상 줄지 않고 평탄해지는 상태 |
| capacity | "캐파시티" | 모델이 담을 수 있는 패턴의 양. 폭·깊이가 클수록 ↑ |
| ablation | "어블레이션" | 한 요소만 빼서 효과를 분리해 측정하는 비교 실험 |

---

## 슬라이드 1 · 표지 (10초)

> 안녕하세요. **SW-AI 1조** 박지용입니다.
> 저희는 MNIST를 **NumPy로** 구현하고, **제공된 스켈레톤 코드를 채워, Baseline 모델에서 시작해 팀원의 회고를 거쳐 개선 모델 v2까지** 두 단계로 진행한 과정을 공유하겠습니다.

---

## 슬라이드 2 · 과제 & 목표 (15초)

> MNIST는 손글씨 숫자 10개 클래스 분류 문제입니다.
> Forward, Loss, Backward, Update 전 사이클을 NumPy로 직접 짜는 것이 목표였고, **Test 정확도 권장치 97% 이상**과 **단위 테스트 21개 통과**를 기준으로 잡았습니다.

---

## 슬라이드 3 · Baseline — 만들어 보니, 그리고 회고에서 보인 2가지 개선 포인트 (35초)

> **먼저 Baseline 모델을 만들었습니다.**
> 은닉층 512와 256, 드롭아웃 0.3, learning rate 1e-3 고정, 15 epoch — 결과는 **Test 98.41%**로 권장 목표 97%를 넘겼습니다.
> 그런데 학습 과정을 자세히 회고해보니, **두 가지 개선 포인트**가 보였습니다.
> **첫째 — 후반 loss 포화.** epoch 13에서 15까지 loss가 0.025 근방에서 **3 epoch 연속 거의 평탄**해집니다 — **새추레이트(saturate, 포화)** 상태죠.
> **둘째 — Train/Test 1.44%p 격차.** train 99.85% vs test 98.41%로 경미한 과적합 신호. train이 거의 100%까지 올라온 만큼 일반화 여력이 있다는 뜻이고요.
> 한 가지 짚자면, **train 99.85% 자체는 문제가 아닙니다.** 다음 슬라이드에서 보겠지만, 해결 방법 ②의 부작용을 보완하기 위해 모델 폭을 **설계 페어링**으로 함께 키웁니다.

---

## 슬라이드 4 · 개선 방향 — 2가지 해결 방법 + 1가지 설계 페어링 (50초)

> 두 개선 포인트에 **1:1 해결 방법 2개**, 그중 **해결 방법 ②의 부작용을 보완하는 설계 페어링 1개**를 묶었습니다.
> **해결 방법 ① — 포화 신호에는 learning rate step decay.** epoch 10에서 11 사이에 lr을 1e-3에서 1e-4로 줄여, 후반에 fine-tuning 단계를 만드는 표준 전략입니다.
> **해결 방법 ② — 격차 신호에는 Dropout 0.3 → 0.4.** 정규화를 강화해 일반화를 끌어올리는 의도입니다.
> 그리고 한 가지 더. **은닉층 폭 2배는 새 문제의 해결 방법이 아니라, 해결 방법 ②의 부작용을 보완하는 설계 페어링입니다.**
> Dropout을 강하게 걸면 forward마다 살아남는 뉴런이 줄어 effective capacity가 감소하니, 그걸 폭 증가로 보상하는 거죠 — **"정규화 강화 + capacity 증가"는 표준 페어링**입니다.
> 부수적으로 baseline의 train 99.85%가 진짜 capacity 상한인지도 함께 확인됩니다.
> **헤(He)와 자비에(Xavier) 초기화 비교는 제외**했습니다. He는 ReLU에 맞춰진 표준이고, Xavier는 sigmoid/tanh용이라 ReLU에선 vanishing 가능성이 커집니다. 이론적으로 He 우위가 명백해, 단독 비교는 위 세 변경의 효과 확인보다 우선순위가 낮다고 판단했습니다.
> 세 변경(해결 방법 2 + 페어링 1)을 한 번에 적용한 결과물을 **"개선 모델 v2"**라고 부릅니다. "Best"가 아닌 이유는 단 한 번의 비교에서의 우승자일 뿐이라 과한 표현이라고 봤기 때문입니다.

---

## 슬라이드 5 · 모델 구조 — Baseline vs v2 (20초)

> 두 모델의 구조 차이입니다.
> Baseline은 은닉층 폭 **512와 256, Dropout 0.3**, v2는 **1024와 512, Dropout 0.4**입니다.
> 출력층(어파인 10 + 소프트맥스)과 헤(He) 초기화는 두 모델 동일, 파라미터 수는 **약 2.5배** (53만 → 134만)로 늘었습니다.

---

## 슬라이드 6 · 학습 설정 — Baseline vs v2 (15초)

> Dropout은 앞 슬라이드에서 봤으니, 학습 설정에서 **또 한 가지 차이는 learning rate 스케줄링**입니다.
> Baseline은 lr 1e-3 고정, v2는 **epoch 1–10은 1e-3, 11–15는 1e-4로 줄이는 step decay**입니다.
> 옵티마이저 아담(Adam), 배치 크기 128, seed 42, 미니배치 4단계 사이클은 두 모델 모두 동일합니다.

---

## 슬라이드 7 · 학습 손실 — 해결 방법 ①의 효과 (30초)

> 학습 손실 그래프입니다. 회색이 Baseline, 파란색이 v2입니다.
> 가장 주목할 부분은 **epoch 10과 11 사이**입니다. v2의 학습률을 1e-3에서 1e-4로 줄인 직후 loss가 **-31% 추가 감소**했습니다.
> Baseline은 같은 구간에서 거의 평탄합니다.
> 즉 **해결 방법 ①(lr step decay)이 의도대로 saturation(포화)을 풀어냈다**는 직접 증거입니다.
> 최종적으로 v2의 loss는 **Baseline 대비 약 40% 더 낮은 수준**까지 내려갔습니다.

---

## 슬라이드 8 · 최종 정확도 — 세 변경의 종합 효과 (30초)

> 정확도 비교입니다.
> Baseline **Test 98.41%** → v2 **Test 98.62%**, **+0.21%p 향상**입니다.
> 더 의미 있는 신호로, **Train/Test 격차도 1.44%p에서 1.33%p로 좁혀져 — overfitting도 함께 완화**됐습니다. 해결 방법 ②(Dropout 강화)의 의도가 작동한 증거죠.
> 다만 모델이 커지면서 **학습 시간은 약 6배** — 정확도 향상의 비용입니다.
> **이상으로 발표 마치겠습니다. 감사합니다.**

---

## 백업 슬라이드 (Q&A 대비, 본 발표 시간 외)

아래 슬라이드 9–11은 본 발표에 포함하지 않지만, Q&A 대응을 위해 스크립트는 그대로 유지합니다.

---

## 슬라이드 9 · 변경별 효과 회고 (25초)

> 세 변경(해결 방법 2 + 페어링 1)이 각자 의도대로 작동했는지 회고합니다.
> **해결 방법 ① lr step decay — 의도대로, +0.21%p 향상의 거의 전부를 단독으로 만들어낸 변경**으로 데이터에서 확인됐습니다. 부록 E의 v2 phase 1(lr=1e-3, 10 epoch) 정확도 98.41%가 baseline 15 epoch 최종(98.41%)과 정확히 같습니다 — 즉 향상은 사실상 lr decay 단계에서 나왔습니다.
> **해결 방법 ② Dropout 0.4 — 방향은 맞지만 효과는 제한적**. 격차가 0.11%p만 줄었기 때문에, 더 큰 폭이나 데이터 증강과 조합할 여지가 있습니다.
> **페어링, 폭 2배 — 의도대로 동반자 역할**. 단독 효과는 작지만 애초에 단독 효과를 노린 변경이 아니라 해결 방법 ②의 effective capacity 보상이 목적이었으니, 정확히 그 자리에서 작동했습니다.

---

## 슬라이드 10 · 구현하며 배운 핵심 (20초)

> 마지막으로 **구현하며 배운 핵심 4가지**입니다.
> ① **in-place 갱신**(`-=`) — layer와 optimizer는 같은 ndarray를 참조하므로 `-=`는 그 배열을 직접 수정해 양쪽 모두 갱신. `=`로 재할당하면 dict만 새 배열을 가리키고 layer는 옛 배열을 봐서 학습이 끊깁니다.
> ② **train/test 모드 분리** — BatchNorm은 train에서 배치 통계로 정규화하고 test는 누적된 running 통계 사용, Dropout은 train에서만 mask + scale. 잘못 쓰면 정확도 폭락.
> ③ **Softmax + Cross Entropy 결합 미분** — 따로 미분하면 softmax 출력이 분모에 끼어 식이 복잡한데, 합치면 깨끗이 약분돼 `(y_pred − y_true) / N` 한 줄로 정리됩니다.
> ④ **Shape 매칭** — gradient의 shape이 파라미터와 일치해야 합니다. 어긋나면 broadcasting으로 silent하게 잘못 갱신돼 학습은 굴러가는데 결과만 이상해지죠 — backward 디버깅 1차 관문이었습니다.

---

## 슬라이드 11 · 한계 & 향후 (10초)

> 한계는 **세 변경을 동시에 적용해 효과 분리가 어렵다**는 점, 그리고 학습 시간 trade-off입니다.
> 다음엔 **ablation 실험**으로 단독 효과를 정량화하고, 더 깊은 네트워크와 데이터 증강을 시도해 보겠습니다. **이상으로 발표 마치겠습니다. 감사합니다.**

---

## 시간 배분 합계

**본 발표 (1–8번)**

| 슬라이드                  | 시간 (초)         |
| --------------------- | -------------- |
| 1 표지                  | 10             |
| 2 과제 & 목표             | 15             |
| 3 Baseline 결과 & 회고    | 35             |
| 4 개선 방향 — 2 해결 방법 + 1 페어링 | 50           |
| 5 모델 구조 비교            | 20             |
| 6 학습 설정 비교            | 15             |
| 7 학습 손실               | 30             |
| 8 최종 정확도 + 마무리        | 30             |
| **본 발표 합계**           | **205초 (약 3분 25초)** |

**백업 / Q&A 대비 (시간 외, 참고)**

| 슬라이드                  | 시간 (초)         |
| --------------------- | -------------- |
| 9 변경별 효과 회고           | 25             |
| 10 구현하며 배운 핵심         | 20             |
| 11 한계 & 마무리           | 10             |

---

## 발표 시 체크 포인트

- **핵심 서사**: "Baseline 만들었더니 → 회고에서 2가지 개선 포인트 → 1:1 해결 방법 2개 + 해결 방법 ②의 부작용을 보완하는 설계 페어링 1개 → v2 → 변경별 회고". 이 흐름이 발표의 중심선
- **숫자보다 인과**: 슬라이드 3·4에서 "신호 → 해결 방법", "해결 방법 ②의 부작용 → 페어링"의 매핑이 듣는 사람에게 가장 인상 깊은 부분 → 천천히
- **그래프 짚기**: 슬라이드 7에서 손가락/레이저로 **epoch 10→11 전환점**을 짚어주면 해결 방법 ①의 효과가 시각적으로 와닿음
- **숫자 외울 필요 없음**: 98.41%, 98.62%, +0.21%p, -31%, 1.44%p → 1.33%p 정도만 자연스럽게
- **고유명사 읽기 통일**: 처음 등장할 때 "헤(He)", "자비에(Xavier)", "learning rate(학습률)"처럼 한 번만 풀어 읽고, 이후엔 짧게
- **"v2" 호명**: 슬라이드 4에서 "개선 모델 (v2)"로 도입한 뒤, 이후엔 "v2"로 짧게
- **"99.85%는 문제가 아니다"를 명시**: 슬라이드 3·4를 거치며 "train 99.85% 자체는 문제가 아니지만, Dropout 0.4 강화의 부작용을 보완하기 위한 페어링으로 폭을 키웠다"는 점을 한 번 또렷이 말해줄 것 — 이 부분이 피어 비평을 받았던 지점
- **Q&A 대비 핵심 질문**:
  - Q: "왜 한꺼번에 적용했나?" → A: "단일 4분 발표 분량 제약 + 시간 한정. ablation은 향후 과제로 명시. 다만 부록 E에서 v2 phase 1 = baseline 최종 정확도로 **lr decay의 기여는 거의 단독으로 분리 가능**했습니다"
  - Q: "왜 폭을 2배로? 99.85%가 문제도 아닌데?" → A: "**문제 해결이 아니라 해결 방법 ②(Dropout 강화)의 부작용 보완.** Dropout 강해지면 effective capacity가 줄어드니 폭으로 보상한 표준 페어링"
  - Q: "v2가 진짜 최선인가?" → A: "단 1회 비교의 우승자이므로 'Best'는 과한 표현. 그래서 v2"
  - Q: "**epoch을 더 늘려보지 그랬나?**" → A: "**부록 E에 epoch별 test 정확도 곡선을 측정해 두었습니다.** Baseline은 epoch 11에서 98.39%로 peak 찍은 뒤 12–15에서 98.27–98.41% 사이 진동만, v2도 lr decay 직후 98.59%로 점프한 뒤 98.58–98.62% 사이로 saturate. 두 모델 모두 epoch 추가로 의미 있는 향상은 어렵다는 게 데이터로 보입니다"
  - Q: "epoch 학습 시간은 비싸지 않나?" → A: "v2는 epoch당 ~90초라 추가 5 epoch이면 +7.5분. 위 데이터로는 그 비용을 들여도 정확도는 거의 안 변하는 saturate 구간"
