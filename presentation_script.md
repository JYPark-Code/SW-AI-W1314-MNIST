# 발표 스크립트 (4분 / 240초)

> 슬라이드와 같은 순서. 괄호 안은 권장 발표 시간.
> 발표자가 직접 외울 필요는 없고, 핵심 흐름과 페이스 가이드용.

---

## 발음 / 용어 가이드 (먼저 한 번 훑어보기)

| 용어 | 읽는 법 | 보충 설명 |
| -- | -- | -- |
| **He** 초기화 | **"헤"** 초기화 | Kaiming He(중국계 ML 연구자)의 이름. ReLU 표준 초기화 |
| **Xavier** 초기화 | **"자비에"** 초기화 | Xavier Glorot(프랑스 ML 연구자)의 이름. Sigmoid/tanh 표준 |
| **lr** / learning rate | "학습률" / "엘 알" | 한 번 업데이트 시 gradient에 곱해주는 값. 본 발표에선 lr ≈ "학습률" |
| **step decay** | "스텝 디케이" / "단계적 감소" | 학습률을 일정 epoch마다 한 번씩 줄이는 방식 |
| Affine | "어파인" | 완전연결층 (`y = xW + b`) |
| ReLU | "렐루" | Rectified Linear Unit, 음수→0인 활성화 함수 |
| Softmax | "소프트맥스" | 확률 분포로 변환하는 출력 함수 |
| Cross Entropy | "크로스 엔트로피" | 분류 손실 함수 |
| BatchNorm | "배치놈" (= 배치 정규화) | 배치 단위 정규화 layer |
| Dropout | "드롭아웃" | 학습 중 일부 뉴런 무작위 차단 |
| Adam | "아담" | 모멘텀 + RMSProp 결합 옵티마이저 |
| epoch | "에폭" | 전체 학습 데이터를 한 번 다 본 단위 |
| batch | "배치" | 한 번에 학습하는 샘플 묶음 (여기선 128개) |
| gradient | "그래디언트" / "기울기" | 손실의 파라미터별 미분 |
| in-place | "인플레이스" / "제자리" | 새 배열을 만들지 않고 같은 배열의 값을 직접 바꾸는 연산 |
| saturate | "새추레이트" / "포화" | 손실이 더 이상 줄지 않고 평탄해지는 상태 |

---

## 슬라이드 1 · 표지 (15초)

> 안녕하세요. **SW-AI 1조** 박지용, 김석제, 서원규, 김진호입니다.
> 저희는 MNIST 손글씨 인식 과제를 PyTorch나 TensorFlow 같은 프레임워크 없이, **NumPy만으로 신경망을 직접 구현**했고, 최종적으로 **Test 정확도 98.62%**를 달성했습니다.

---

## 슬라이드 2 · 과제 & 목표 (20초)

> MNIST는 손글씨 숫자 0부터 9까지 10개 클래스로 분류하는 문제입니다.
> 저희는 **Forward, Loss, Backward, Update 전 사이클을 직접 짜는 것**이 목표였고, Test 정확도 95% 이상, 권장 97% 이상을 목표치로 잡았습니다.
> 검증은 학습 정확도뿐 아니라 각 layer와 optimizer의 동작을 확인하는 **단위 테스트 21개**까지 모두 통과시키는 것을 기준으로 했습니다.

---

## 슬라이드 3 · 접근 방식 — 두 모델 (25초)

> 본격적인 결과로 넘어가기 전에, 저희가 어떤 흐름으로 진행했는지 먼저 짚고 가겠습니다.
> 저희는 **두 단계로 접근**했습니다.
> 먼저 **Baseline 모델**을 만들어 권장 목표 97%를 달성할 수 있는지 확인했고,
> 그 회고에서 도출한 **개선 후보 4가지** — ① 드롭아웃 강화, ② **learning rate, 즉 학습률 스케줄링**, ③ 은닉층 폭 변경, ④ 헤(He)와 자비에(Xavier) 초기화 비교 — 중에서
> 효과가 클 것으로 본 **3가지를 한 번에 적용해 Best 모델**을 만들었습니다.
> 이후 슬라이드의 모든 비교는 **이 두 모델 — Baseline과 Best — 기준**입니다.

---

## 슬라이드 4 · 모델 구조 — Baseline vs Best (30초)

> 두 모델의 구조 차이입니다.
> Baseline은 은닉층 폭이 **512 / 256**, 드롭아웃은 **0.3**입니다.
> Best는 은닉층 폭을 **1024 / 512로 약 두 배**로 키우고, 드롭아웃을 **0.4**로 올렸습니다.
> 출력층(어파인 10 + 소프트맥스)과 가중치 **헤(He) 초기화**는 두 모델 동일합니다.
> 파라미터 수는 537,354개에서 **약 134만 개로 약 2.5배** 증가했습니다.

---

## 슬라이드 5 · 학습 설정 — Baseline vs Best (30초)

> 학습 설정에서 두 모델의 차이는 **learning rate, 즉 학습률 스케줄링** 한 가지입니다.
> Baseline은 학습률을 **1e-3으로 15 epoch 동안 고정**했고요,
> Best는 **step decay** — 즉 단계적 감소 — 방식으로 **epoch 1~10은 1e-3, 11~15는 1e-4로 한 번 줄였습니다**.
> 옵티마이저는 아담(Adam), 배치 크기 128, 총 15 epoch, seed 42까지는 두 모델 모두 동일합니다.
> 미니배치마다 **Forward, Loss, Backward, Update** 네 단계를 반복하는 사이클도 동일합니다.

---

## 슬라이드 6 · 학습 손실 — Baseline vs Best (35초)

> 학습 손실 그래프입니다. **회색이 Baseline, 파란색이 Best**입니다.
> 두 곡선 모두 첫 epoch에서 가장 큰 감소를 보이고 단조 감소합니다.
> 가장 주목할 부분은 **epoch 10과 11 사이**입니다. 이 시점에서 Best의 학습률을 1e-3에서 1e-4로 줄였는데, loss가 **0.0375에서 0.0257로, -31% 추가 감소**했습니다.
> 평소 같으면 후반에 새추레이트(saturate), 즉 포화되는 구간인데, 스케줄링 덕분에 **추가 수렴 여력을 끌어낼 수 있었습니다**.
> 마지막 epoch loss는 Baseline 0.0252, **Best 0.0154**입니다.

---

## 슬라이드 7 · 최종 정확도 — Baseline vs Best (25초)

> 최종 정확도 비교입니다.
> Baseline은 **Test 98.41%**, Best는 **Test 98.62%**로 **+0.21%p 향상**됐습니다.
> Train과 Test의 격차도 1.44%p에서 1.33%p로 살짝 좁혀졌고요.
> 단, Best는 모델이 커지면서 **학습 시간이 약 4분에서 23분으로, 약 6배 증가**했습니다 — 정확도 향상에 대한 비용입니다.
> 단위 테스트 21개는 두 모델 모두 통과 상태입니다.

---

## 슬라이드 8 · 어떤 변경이 가장 효과적이었나 (30초)

> 적용한 세 가지 변경 중 어느 것이 가장 컸을까요.
> **첫째, learning rate step decay가 가장 또렷한 효과**였습니다. 후반에 saturate되던 구간에서 -31% 추가 감소를 끌어낸 것이 이 덕분입니다.
> **둘째, 은닉층 폭 증가**는 모델 capacity를 키워 train 정확도를 99.85%에서 99.95%로 올렸지만, 단독으로는 큰 차이를 만들지 못했고 다른 변수와 함께 시너지를 냈습니다.
> **셋째, 드롭아웃 0.3에서 0.4로의 변경**은 train과 test의 격차를 좁히는, 일반화 향상에 기여했습니다.

---

## 슬라이드 9 · 구현하며 배운 핵심 (25초)

> 구현하며 배운 핵심은 네 가지입니다.
> 파라미터는 **제자리(in-place)로 갱신**해야 layer가 따라옵니다 — `=` 재할당하면 dict만 바뀌고 layer는 옛 배열을 보게 됩니다.
> **배치놈과 드롭아웃은 학습과 추론 동작이 다르고**,
> **소프트맥스와 크로스 엔트로피 결합 미분**은 `(y_pred − y_true) / N`으로 깔끔하게 정리되고,
> **shape 매칭**이 backward 디버깅의 첫 관문이라는 점이 기억에 남습니다.

---

## 슬라이드 10 · 한계 & 향후 과제 (5초)

> 한계는 정확도 0.21%p를 위해 학습 시간이 약 6배 늘었다는 점, MLP로는 98.6%대가 상한이라는 점입니다.
> 다음엔 더 깊은 네트워크와 데이터 증강을 시도해 보겠습니다. **이상으로 발표 마치겠습니다. 감사합니다.**

---

## 시간 배분 합계

| 슬라이드           | 시간 (초) |
| -------------- | ------ |
| 1 표지           | 15     |
| 2 과제 & 목표      | 20     |
| 3 접근 방식 — 두 모델 | 25     |
| 4 모델 구조        | 30     |
| 5 학습 설정        | 30     |
| 6 학습 손실        | 35     |
| 7 최종 정확도       | 25     |
| 8 기여 요소        | 30     |
| 9 배운 점         | 25     |
| 10 마무리         | 5      |
| **합계**         | **240초 (4분)** |

---

## 발표 시 체크 포인트

- **속도**: 표지·마무리는 천천히, 모델 구조·학습 설정은 약간 빠르게 (정보량 多)
- **포인트 강조**: 그래프 보여줄 때 손가락/레이저로 **epoch 10→11 전환점**을 짚어주면 임팩트 ↑
- **숫자 외울 필요 없음**: 98.62%, +0.21%p, -31% 세 숫자만 자연스럽게
- **고유명사 읽기 통일**: 처음 등장할 때 "헤(He)", "자비에(Xavier)", "learning rate(학습률)"처럼 한 번만 풀어 읽고, 이후엔 짧게
- **두 모델 명칭 통일**: "Baseline"과 "Best" — 슬라이드 3에서 한 번 정의해 두면 이후 슬라이드에서 짧게 써도 청중이 따라옴
